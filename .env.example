# Mock phase (spec/19 §1): mock is the only allowed value. No database, no Docker.
DATA_SOURCE=mock

# Fixed "today" for the mock layer (spec/19 §4). The mock never calls new Date() without it.
MOCK_TODAY=2026-09-15
