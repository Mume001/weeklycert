// @wc/core: pure functions, no I/O (spec/09 §3). The engine and the findings
// catalogue arrive in step 2 (session B); this is the date and week logic the
// app shell needs now.
export * from './dates.ts'
export * from './weeks.ts'
