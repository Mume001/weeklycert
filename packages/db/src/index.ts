// @wc/db: Drizzle schema, SQL migrations with RLS, seed (spec/04, spec/09 §3).
// Empty on purpose. Step 4 starts only after the gate in spec/12
// ("imamo 10 uplata"). Until then nothing may import Drizzle or pg.
export {}
